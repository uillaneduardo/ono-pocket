const screenButtons=[...document.querySelectorAll('[data-screen-target]')];const screens=[...document.querySelectorAll('.ono-screen')];const navButtons=[...document.querySelectorAll('[data-nav-screen]')];function showScreen(id){screens.forEach(s=>s.classList.toggle('active',s.id===`ono-screen-${id}`));screenButtons.forEach(b=>b.classList.toggle('active',b.dataset.screenTarget===id));navButtons.forEach(b=>b.classList.toggle('active',b.dataset.navScreen===id));document.querySelector('.ono-screen.active')?.scrollTo(0,0)}screenButtons.forEach(b=>b.addEventListener('click',()=>showScreen(b.dataset.screenTarget)));navButtons.forEach(b=>b.addEventListener('click',()=>showScreen(b.dataset.navScreen)));document.querySelectorAll('.map-node[data-location]').forEach(n=>n.addEventListener('click',()=>{document.querySelectorAll('.map-node').forEach(x=>x.classList.remove('selected'));n.classList.add('selected');const name=n.dataset.location;document.querySelector('#map-location-name').textContent=name;document.querySelector('#map-location-panel').hidden=false}));const sheet=document.querySelector('#ono-context-sheet');const backdrop=document.querySelector('#ono-sheet-backdrop');function setSheet(open){sheet?.classList.toggle('open',open);backdrop?.classList.toggle('open',open)}document.querySelectorAll('[data-open-sheet]').forEach(b=>b.addEventListener('click',()=>setSheet(true)));document.querySelectorAll('[data-close-sheet]').forEach(b=>b.addEventListener('click',()=>setSheet(false)));backdrop?.addEventListener('click',()=>setSheet(false));document.querySelector('#travel-button')?.addEventListener('click',()=>{showScreen('activities');const banner=document.querySelector('#travel-activity');if(banner){banner.hidden=false}});showScreen('map');